/**
 * إعدادات المنصة القابلة للضبط.
 *
 * المكان الوحيد اللي تتغيّر منه الأرقام دي. متكتبش أي رقم منهم
 * مباشرة في أي ملف تاني — عشان تفضل قابلة للمراجعة من نظرة واحدة.
 *
 * ملاحظة: دي إعداداتنا إحنا. أي أرقام مشابهة في لوحة Supabase
 * (طول رمز المصادقة، حدود إرسال البريد) بتخصّ نظام Supabase Auth
 * اللي إحنا مش مستخدمينه، وماليهاش أي أثر هنا.
 */
export const config = {
  /** تأكيد بريد التاجر */
  otp: {
    /** عدد أرقام الرمز — لازم يطابق عدد الخانات في صفحة التأكيد */
    length: 6,
    /** مدة صلاحية الرمز بالدقائق */
    ttlMinutes: 10,
    /** أقصى عدد محاولات خاطئة قبل إبطال الرمز */
    maxAttempts: 5,
    /** المهلة بين طلب رمز والتالي، بالثواني */
    resendCooldownSeconds: 60,
  },

  /** الجلسات */
  session: {
    /** مدة بقاء المستخدم مسجّلًا، بالأيام */
    days: 30,
  },

  /** التجربة المجانية للمتجر الجديد، بالأيام */
  trialDays: 14,
} as const
