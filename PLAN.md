# خطة بناء زاوية

الملف ده هو ذاكرة المشروع. أي جلسة شغل جديدة تبدأ من هنا: تقرأ الحالة، تكمّل
البند التالي، وتحدّث الحالة قبل ما ترفع.

**الحالة:** `[ ]` لسه · `[~]` شغّال عليه · `[x]` خلص ومتحقَّق منه

---

## القرارات المعتمدة

| البند | القرار |
|---|---|
| السوق | مصر أولًا. الخليج لاحقًا |
| الشريحة | أي تاجر، أي مرحلة، أي حجم |
| الاسم | «زاوية / zawya» — **مؤقت**، قابل للتغيير من مكان واحد |
| السكوب | تعادُل كامل مع المنافس + تفوّق، مش MVP مقصوص |
| المنفّذ | Claude، بمفرده |
| الاستضافة | Vercel + Supabase |

**اللي بيبقى ناقصًا حتى بعد اكتمال الكود:** الحسابات نفسها. إحنا **مش** بنتعاقد
مع بوابة ولا شركة شحن — التاجر بيفتح حسابه عندهم بنفسه وبيلزق مفاتيحه من صفحة
الدفع أو الشحن. الكود بينادي الـAPI الحقيقي بمفاتيحه، لكن **مسار كل مزوّد**
ما اتجرّبش بحساب حقيقي لسه (اتجرّب بمفاتيح وهمية: النداء بيوصلهم فعلًا
والرفض بيرجع بسببه بالعربي). أول تاجر بيربط مزوّد لازم يتراقب أول طلب منه.

---

## المرحلة صفر — الأساس

- [x] تجهيز المشروع (Next 16 · React 19 · Tailwind 4 · TypeScript)
- [x] مخطط قاعدة البيانات الكامل — 40+ جدول في 11 ملفًا
- [x] عميل قاعدة البيانات مع تجميع اتصالات مناسب لـSupabase
- [x] توجيه متعدد المتاجر بالمضيف (`src/proxy.ts`)
- [x] نظام التصميم وهوية زاوية (ألوان الشعار، وضع فاتح وداكن، RTL)
- [x] أدوات العملة والهواتف والتواريخ
- [x] الموقع التعريفي — الصفحة الرئيسية
- [x] ربط Vercel والنشر التلقائي — zawyaeg.site
- [x] تطبيق المخطط على Supabase — 65 جدولًا، ومتحقَّق من التسجيل والدخول فعليًا

## المرحلة الأولى — متجر بيبيع

- [x] تسجيل ودخول التاجر + الجلسات
- [x] إنشاء متجر (اختيار النطاق الفرعي، الإعدادات الأولية)
- [x] هيكل لوحة التحكم (تخطيط، تنقّل، حماية المسارات)
- [x] الأقسام والمنتجات والمتغيّرات (CRUD كامل + رفع صور)
- [x] المخزون والحركات
- [x] واجهة المتجر: الرئيسية، الأقسام، صفحة المنتج، البحث
- [x] السلة (مخزّنة محليًا + متزامنة للعميل المسجّل)
- [x] الشيك أوت + إعداداته القابلة للتخصيص
- [x] **التقاط الطلب الناقص لحظة كتابة الرقم**
- [x] الدفع عند الاستلام + الطلبات + صفحة الشكر
- [x] مناطق وأسعار الشحن
- [x] لوحة التحكم: نظرة عامة بأرقام حقيقية

## المرحلة الثانية — التوسّع

- [~] العملاء — قائمة التاجر تمّت (إنفاق، مستوى، واتساب/اتصال). حساباتهم على المتجر لسه
- [ ] الكوبونات والعروض والباقات
- [ ] محرّك الرسائل (قوالب، طابور، سجل)
- [ ] تكامل واتساب + SMS + تيليجرام
- [ ] تذكيرات السلة المتروكة
- [ ] رمز التحقق (OTP) قبل تأكيد الطلب
- [x] بوابات الدفع: ٨ بوابات بمفاتيح التاجر (باي موب، فوري، كاشير، ماي فاتورة،
      تابي، تمارا، سترايب، باي بال) — جلسة دفع حقيقية + ويب هوك موقّع
- [x] شركات الشحن: ٥ بربط تلقائي (بوسطة، مايلرز، J&T، أرامكس، شيب بلو) + ٣ يدوي
      (R2S، سبرينت، VOO) — تسجيل الشحنة تلقائي وتحديث الحالة من الويب هوك
- [ ] البكسلات (فيسبوك، تيك توك، سناب، جوجل)
- [ ] CAPI من الخادم مع منع ازدواج الأحداث
- [x] محرر الثيم بمعاينة حيّة
- [ ] المرتجعات والاستبدال

## المرحلة الثالثة — التميّز

- [ ] الولاء والنقاط والمستويات
- [ ] عجلة الحظ
- [ ] تطبيق الولاء (PWA)
- [ ] محرّك الأتمتة بشروط
- [ ] تحليلات الربح الحقيقي
- [ ] صفحات البيع (الفانلز)
- [ ] المدوّنة والصفحات الثابتة
- [ ] اختبار A/B
- [ ] المسوّقون بالعمولة والإحالات
- [x] مزامنة الكتالوج — ملف عام لميتا وجوجل وتيك توك، من غير أي تعاقد
- [ ] الدروب شيبينج
- [ ] API عام + ويب هوكس + مفاتيح
- [x] الحجوزات والمواعيد — تقويم في المتجر، ومواعيد عمل ولوحة في اللوحة
- [x] الاشتراكات — باقات، وتحصيل بمحفظة/إنستا باي، وقفل مميزات
- [x] لوحة إدارة المنصة — بحث بمعرّف الحساب وتفعيل/إلغاء بضغطة

---

## المزوّدون الخارجيون — إزاي الحتة دي مركّبة

| الملف | مسؤوليته |
|---|---|
| `src/lib/providers.ts` | سجل المزوّدين وحقولهم بالظبط زي ما الشركة بتسمّيها |
| `src/lib/provider-store.ts` | حفظ وقراءة — الأسرار مشفّرة، والعلني في `config.values` |
| `src/lib/integrations/payments.ts` | إنشاء جلسة الدفع عند كل بوابة |
| `src/lib/integrations/shipping.ts` | تسجيل الشحنة عند كل شركة |
| `src/lib/payment-dispatch.ts` | بيربط الطلب بالبوابة ويسجّل المحاولة |
| `src/lib/shipment-dispatch.ts` | بيربط الطلب بالشركة وبيسجّل الشحنة |
| `src/lib/order-flow.ts` | تحوّلات حالة الطلب والشحنة — **بمعزل عن جلسة التاجر** |
| `src/app/api/webhooks/{pay,ship}/…` | الإشعارات الواردة، بتحقّق توقيع إلزامي |

**قواعد ملزمة في الحتة دي:**

- **الطلب ما بيتعلّمش «مدفوع» إلا من الويب هوك الموقّع.** رجوع العميل من صفحة
  البوابة مجرد رابط — أي حد يفتحه.
- **تحوّلات الحالة كلها من `order-flow`**، لأن الويب هوك مالوش جلسة تاجر
  و`getDashboardContext` بيوجّه على `/login`.
- **فشل المزوّد ما بيلغيش الطلب أبدًا.** بيتسجّل في `payment_attempts` أو
  على حساب الشركة وعلى سجل الطلب، والتاجر بيكمّل بإيده.
- **السعر المعروض والسعر المحصّل من مصدر واحد** (`getDisplayShipping` و
  `shippingFor`) — وإلا الفرق بيطلع من جيب التاجر في كل طلب.
- **الدفع عند الاستلام مفتاحه في إعدادات الشحن** (`shippingZones.codEnabled`)
  لا في طرق الدفع — مفتاح واحد لحاجة واحدة.

## اللي اتبنى فوق الجداول الفاضية

الجداول دي كانت موجودة في المخطط من غير أي كود. دلوقتي بقت شغّالة:

| الجدول | اللي بيشغّله |
|---|---|
| `inventory_locations` + `inventory_levels` | `src/lib/branches.ts` — الفروع وتوزيع المخزون والنقل |
| `job_queue` | `src/lib/jobs.ts` — طابور مهام بقفل ذرّي وتباعد متزايد |
| `payment_attempts` | `dashboard/payments/attempts.tsx` — ليه الدفع فشل |
| `notification_recipients` | `src/lib/notify-team.ts` — مين يتبلّغ لما يحصل إيه |
| `subscriptions` | سجل فترات الاشتراك في صفحة الاشتراك |
| `bookings` | `src/lib/bookings.ts` — تقويم الخدمات في المتجر واللوحة |
| `marketplace_connections` | `src/lib/marketplace.ts` — ملف كتالوج عام |
| `subscription_requests` | `dashboard/admin` — مين دفع ومستني تفعيل |

**قواعد ملزمة في الحتة دي:**

- **الفروع بتقسّم المخزون، مش بتحلّ محلّه.** `products.stock` بيفضل
  اللي المتجر بيبيع منه، والفروع بتقول فين البضاعة قاعدة. لو البيع
  قرا من الفروع، التاجر اللي نسي يوزّع منتجًا كان بيلاقيه «نافد».
- **مواعيد الحجز بتتحسب على الخادم.** لو المتصفح حسبها، اتنين
  بيحجزوا نفس المعاد.
- **ملف الكتالوج عام بالضرورة** — ميتا وجوجل بيجيبوه من غير مصادقة.
  عشان كده مفيش فيه غير منتجات وأسعار، وكلها ظاهرة للزائر أصلًا.
- **الطابور بيتسحب مع المهمة اليومية** لا كمهمة مجدولة لوحده: خطة
  Vercel المجانية بتسمح بمهمة واحدة، والتانية بترفض النشر كله.

## الاشتراك وقفل المميزات — إزاي الحتة دي مركّبة

مفيش بوابة دفع ومش هتبقى فيه. التاجر بيحوّل على محفظة أو إنستا باي
وبيبعت صورة الإيصال على واتساب، والتفعيل **بإيد إدارة المنصة**.

| الملف | مسؤوليته |
|---|---|
| `src/lib/plans.ts` | الباقات: تجربة ٣ أيام · شهري ٥٠٠ · سنوي ٥٥٠٠ (بالقرش) |
| `dashboard/subscription/trial-card` | التاجر بيبدأ تجربته بإيده — من غير موافقة |
| `src/lib/billing.ts` | رقم التحصيل ورقم واتساب ونص الرسالة الجاهزة |
| `src/lib/entitlements.ts` | **البوابة الوحيدة**: مين يقدر يعمل إيه |
| `src/lib/subscription.ts` | فعّل · ألغِ · أنهِ اللي خلص |
| `src/lib/account-id.ts` | معرّف الحساب `ZW-XXXXXXXX` |
| `src/lib/admin.ts` | بريد إدارة المنصة — مرساة جنب عمود `is_platform_admin` |
| `dashboard/subscription` | الباقات وخطوات التحويل وزرار «تم الدفع» |
| `dashboard/admin` | بحث بالمعرّف، وتفعيل/إلغاء، وطلبات مستنية |

**قواعد ملزمة في الحتة دي:**

- **القرار بالتواريخ لا بالحالة.** `stores.status` نص بيتكتب بإيد
  الإدارة وممكن يبقى قديم لو المهمة اليومية اتأخرت. الحقيقة في
  `subscribedUntil` و`trialEndsAt` — التاريخ عدّى يبقى مقفول.
- **المهمة اليومية بتظبّط الحالة، مش بتقفل.** `expireSubscriptions`
  بتخلّي `status` يطابق الواقع؛ القفل نفسه بيحصل في ثانيته من
  `getEntitlements` حتى لو المهمة ما اشتغلتش.
- **البلور مش حماية.** كل ميزة مقفولة ليها فحص على الخادم في كل فعل
  بيغيّر حاجة — `pointer-events-none` بتتشال من أدوات المطوّرين.
- **زرار «تم الدفع» بيسجّل قبل ما يفتح واتساب.** اللي بيقفل الشباك
  قبل ما يبعت بيفضل ظاهر في لوحة الإدارة بمعرّفه ومتجره.
- **الطلب مش اشتراك.** الصف في `subscription_requests` ما بيفعّلش
  حاجة — التفعيل بيغيّر `stores` بإيد الإدارة بس.
- **المتجر الجديد `free` لا `trial`.** التجربة كانت بتتوهب في
  التسجيل، فكل حساب جديد كان بيخرج وكل المميزات مفتوحة عنده ٣ أيام
  من غير ما يطلبها — يعني القفل موجود في الكود ومش شغّال على حد.
  دلوقتي التاجر بيبدأها بإيده من صفحة الاشتراك (`startTrialAction`)،
  فبتتسجّل باسمه وبتاريخها وإحنا عارفين مين بدأها.
- **التجربة self-serve، والمدفوع لا.** التجربة مبتكلّفش حاجة فمفيش
  تحويل يتأكّد منه؛ تعليقها على موافقتنا كان هيخلّي التاجر يستنى
  عشان يجرّب. الباقات المدفوعة عكسها: ما بتلمسش `stores` خالص لحد
  ما الإدارة تفعّل.
- **مرة واحدة بس.** شرط `trialEndsAt IS NULL` — من غيره التاجر
  بيدوس الزرار كل ٣ أيام ويفضل مجرّب للأبد.
- **القفل بيمنع الإنشاء والتعديل لا السيطرة.** التاجر اللي اشتراكه
  خلص لازم يفضل قادر يوقّف صفحة منشورة ويشيل نطاقه.
- **حساب الإدارة كل حاجة مفتوحة عنده** من `isAdminStore` — مربوط
  بالعلامة على المستخدم لا بمعرّف متجر مكتوب في الكود.
- **لوحة الإدارة بتعرض بيانات اشتراك بس** — لا طلبات ولا عملاء ولا
  محتوى أي متجر. العزل في `getDashboardContext` ما اتلمسش.

## السلة — ملكيّة والتقاط

| الملف | مسؤوليته |
|---|---|
| `components/storefront/cart.tsx` | السلة في المتصفح، مختومة بصاحبها |
| `app/s/[store]/cart-actions.ts` | تسجيل السلة كسلة متروكة لحظة الإضافة |

**قواعد ملزمة في الحتة دي:**

- **السلة مختومة بصاحبها** (`{ owner, items }` في `localStorage`).
  من غير الختم ده، سلة اللي سجّل خروجه كانت بتفضل موجودة، واللي
  يدخل بعده على نفس الجهاز كان بيلاقي منتجات مش بتاعته.
- **ضيف ← حساب = انتقال. أي تغيير تاني = تفضية.** الشيك أوت بيطلب
  الدخول، فالعميل اللي حطّ في سلته وراح يكمّل بيتطلب منه يسجّل —
  ولو فضّينا سلّته ساعتها، كل بيعة من عميل جديد بتضيع عند آخر خطوة.
- **الالتقاط لحظة الإضافة لا لحظة الشيك أوت.** اللي بيسيب سلته من
  غير ما يوصل للشيك أوت أصلًا — وده أغلب اللي بيسيبوا — كان بيختفي
  تمامًا من لوحة التاجر.
- **العميل المسجَّل بس.** الالتقاط محتاج رقم؛ الزائر مالوش واحد،
  وصف من غير وسيلة تواصل مالوش لازمة.
- **بصمة السلة بتمنع إعادة الكتابة.** من غيرها كل تنقّل بين صفحات
  المتجر كان بيعيد التسعير والكتابة. وكمان بتخلّي `abandonedAt` على
  وقت آخر تغيير حقيقي — وهو المعنى الصح لـ«ساب سلته من ساعة».
- **رمز المسوّدة واحد** (`zw_draft_<store>`): السلة والشيك أوت
  بيكتبوا على نفس الصف، وإلا التاجر يلاقي سلتين متروكتين لعميل واحد.

## ملاحظات تقنية للجلسات القادمة

- **الأموال:** كل المبالغ `integer` بالقرش. لا تستخدم `float` أبدًا.
- **النِسَب:** نقاط أساس (`bps`) — 1400 = 14%. تفادي كسور النسب.
- **الطلب الناقص:** `orders.isIncomplete = true` مع `status = 'incomplete'`.
  يتحوّل لطلب حقيقي عند الإكمال، ويُحسب في «المتروكة» غير كده.
- **منع ازدواج البكسل:** `orders.eventId` و`storeEvents.eventId` نفس القيمة
  تُرسل من المتصفح ومن الخادم.
- **الأسرار:** تُشفّر قبل التخزين في `store_plugins.secrets` و`payment_methods.credentials`.
- **الوقت:** كل الطوابع `withTimezone`. العرض بتوقيت المتجر لا بتوقيت الخادم.
- **حدود غير المشترك:** ٥ طلبات (الناقصة مش محسوبة)، وأدوات الذكاء
  وصفحات الهبوط والنطاق المخصص مقفولين.
- **الهجرات:** المخطط بيتطبّق بـ`db:push` لا بـ`migrate`، فلقطة
  `drizzle/meta` بتبقى متأخرة عن القاعدة. أي ملف هجرة مكتوب بإيد
  لازم يبقى idempotent (`IF NOT EXISTS`) — زي `0001_subscriptions.sql`.
