/*
  مفتاح تسجيل الشحنة تلقائيًا عند تأكيد الطلب.

  مكتوب بإيد و idempotent — للسبب المشروح في `0001_subscriptions.sql`.

  **الافتراضي `true` عن قصد.** ده السلوك اللي كان شغّال بلا مفتاح:
  كل تاجر ربط شركة شحن، الشحنة بتتسجّل لحظة التأكيد. لو الافتراضي
  كان `false`، كل المتاجر دي كانت هتلاقي شحناتها وقفت فجأة من غير
  ما حد يغيّر حاجة — والمفتاح المفروض يدّي اختيارًا للي محتاجه، مش
  يغيّر على اللي شغّال.
*/

ALTER TABLE "stores"
  ADD COLUMN IF NOT EXISTS "auto_ship_on_confirm" boolean NOT NULL DEFAULT true;
